{
  "name": "@omni-fs/provider-s3",
  "version": "0.0.0",
  "description": "S3 and S3-compatible (MinIO, R2, B2, Spaces) provider for omni-fs",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "typecheck": "tsc -b --noEmit false --emitDeclarationOnly",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@omni-fs/core": "workspace:*",
    "@aws-sdk/client-s3": "^3.670.0",
    "@aws-sdk/lib-storage": "^3.670.0"
  },
  "devDependencies": {
    "@omni-fs/testing": "workspace:*",
    "@types/node": "^22.8.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.11"
  }
}
