# Strict by default: a package may only import what it declares in its own
# package.json. This is what stops apps/vscode types leaking into packages/*.
hoist=false
strict-peer-dependencies=false
link-workspace-packages=true
